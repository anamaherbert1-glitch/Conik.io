import {NextResponse} from 'next/server'
import {requireWorkspaceRole} from '@/lib/auth/require-user'
export async function GET(){const{supabase,organization}=await requireWorkspaceRole(['owner','admin','editor','viewer']);const{data,error}=await supabase.from('tags').select('id,name,color,created_at').eq('organization_id',organization.id).order('name');if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({tags:data||[]})}
export async function POST(req:Request){const{supabase,organization}=await requireWorkspaceRole(['owner','admin','editor']);const b=await req.json().catch(()=>null);const name=typeof b?.name==='string'?b.name.trim().slice(0,80):'';const color=typeof b?.color==='string'?b.color.trim().slice(0,30):'#111827';if(!name)return NextResponse.json({error:'Le nom de lՎtiquette est obligatoire.'},{status:400});const{data,error}=await supabase.from('tags').insert({organization_id:organization.id,name,color}).select('id,name,color,created_at').single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({tag:data},{status:201})}
