import {NextResponse} from 'next/server'
import {requireWorkspaceRole} from '@/lib/auth/require-user'
export async function GET(){const{supabase,organization}=await requireWorkspaceRole(['owner','admin','editor','viewer']);const{data,error}=await supabase.from('links').select('id,slug,destination_url,funnel_id,created_at').eq('organization_id',organization.id).order('created_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({links:data||[]})}
export async function POST(req:Request){const{supabase,organization}=await requireWorkspaceRole(['owner','admin','editor']);const b=await req.json().catch(()=>null);const destination=typeof b?.destination_url==='string'?b.destination_url.trim():'';let slug=typeof b?.slug==='string'?b.slug.trim().toLowerCase():'';if(!/^https?:\/\//i.test(destination))return NextResponse.json({error:'Destination must be an http or https URL.'},{status:400});if(!slug)slug=Math.random().toString(36).slice(2,9);if(!/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(slug))return NextResponse.json({error:'Invalid slug.'},{status:400});const{data,error}=await supabase.from('links').insert({organization_id:organization.id,funnel_id:b?.funnel_id||null,slug,destination_url:destination}).select('id,slug,destination_url,funnel_id,created_at').single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({link:data},{status:201})}
