'use client'
import Link from 'next/link'
import {useEffect,useState} from 'react'
import {AppShell} from '@/components/app-shell'
export default function NewAutomation(){const[name,setName]=useState(''),[trigger,setTrigger]=useState('new_contact'),[tag,setTag]=useState(''),[tags,setTags]=useState<any[]>([]),[saving,setSaving]=useState(false),[error,setError]=useState('');useEffect(()=>{fetch('/api/tags').then(r=>r.ok?r.json():null).then(j=>setTags(j?.tags||[])).catch(()=>{})},[]);async function save(e:React.FormEvent){e.preventDefault();setSaving(true);setError('');const actions=tag?[{action_type:'add_tag',action_config:{tag_id:tag}}]:[];const r=await fetch('/api/automations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,trigger_type:trigger,trigger_config:{},status:'draft',actions})});const j=await r.json().catch(()=>({}));if(!r.ok){setError(j.error||'Unable to create automation');setSaving(false);return}location.href=`/automations/${j.automation.id}`}return <AppShell active="Automations"><header><div><small>AUTOMATIONS</small><h1>New automation</h1><p className="muted">Create a persisted workflow. Supported internal actions do not send external messages.</p></div></header><section className="panel"><form onSubmit={save} style={{display:'grid',gap:16,maxWidth:620}}><label>Name<input className="form-input" required value={name} onChange={e=>setName(e.target.value)} placeholder="Welcome new leads"/></label><label>Trigger<select className="form-input" value={trigger} onChange={e=>setTrigger(e.target.value)}><option value="new_contact">New contact</option><option value="form_submission">Form submission</option></select></label><label>Add tag after trigger (optional)<select className="form-input" value={tag} onChange={e=>setTag(e.target.value)}><option value="">No tag</option>{tags.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label>{error&&<div className="error">{error}</div>}<div style={{display:'flex',gap:8}}><button className="primary" disabled={saving}>{saving?'Creating…':'Create automation'}</button><Link className="outline" href="/automations">Cancel</Link></div></form></section></AppShell>}
