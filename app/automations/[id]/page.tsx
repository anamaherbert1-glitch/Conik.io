'use client'
import Link from 'next/link'
import {useEffect,useState} from 'react'
import {AppShell} from '@/components/app-shell'
export default function AutomationDetail({params}:{params:Promise<{id:string}>}){const[id,setId]=useState(''),[a,setA]=useState<any>(null),[name,setName]=useState(''),[status,setStatus]=useState('draft'),[error,setError]=useState(''),[saving,setSaving]=useState(false);useEffect(()=>{params.then(p=>setId(p.id))},[params]);useEffect(()=>{if(!id)return;fetch('/api/automations').then(r=>r.json()).then(j=>{const x=(j.automations||[]).find((v:any)=>v.id===id);if(!x){setError('Automation not found.');return}setA(x);setName(x.name);setStatus(x.status)}).catch(()=>setError('Unable to load automation'))},[id]);async function save(e:React.FormEvent){e.preventDefault();setSaving(true);setError('');const r=await fetch(`/api/automations/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,status,trigger_type:a.trigger_type,trigger_config:a.trigger_config||{}})});const j=await r.json().catch(()=>({}));if(!r.ok)setError(j.error||'Unable to save');else setA(j.automation);setSaving(false)}async function remove(){if(!confirm('Delete this automation?'))return;const r=await fetch(`/api/automations/${id}`,{method:'DELETE'});if(r.ok)location.href='/automations';else setError('Unable to delete automation')}if(error&&!a)return <AppShell active="Automations"><div className="error">{error}</div><Link href="/automations" className="outline">Back</Link></AppShell>;return <AppShell active="Automations"><header><div><small>AUTOMATION</small><h1>{a?.name||'Automation'}</h1><p className="muted">Trigger: {a?.trigger_type||'—'} · Actions execute only when the workflow is active.</p></div></header><section className="panel"><form onSubmit={save} style={{display:'grid',gap:16,maxWidth:620}}><label>Name<input className="form-input" value={name} onChange={e=>setName(e.target.value)} required/></label><label>Status<select className="form-input" value={status} onChange={e=>setStatus(e.target.value)}><option value="draft">Draft</option><option value="active">Active</option><option value="paused">Paused</option></select></label>{error&&<div className="error">{error}</div>}<div style={{display:'flex',gap:8}}><button className="primary" disabled={saving}>{saving?'Saving…':'Save changes'}</button><button type="button" className="outline" onClick={remove}>Delete</button><Link className="outline" href="/automations">Back</Link></div></form></section></AppShell>}
