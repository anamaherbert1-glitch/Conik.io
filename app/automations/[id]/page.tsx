'use client'
import Link from 'next/link'
import {useEffect,useState} from 'react'
import {AppShell} from '@/components/app-shell'
export default function AutomationDetail({params}:{params:Promise<{id:string}>}){const[id,setId]=useState(''),[a,setA]=useState<any>(null),[name,setName]=useState(''),[status,setStatus]=useState('draft'),[error,setError]=useState(''),[saving,setSaving]=useState(false);useEffect(()=>{params.then(p=>setId(p.id))},[params]);useEffect(()=>{if(!id)return;fetch('/api/automations').then(r=>r.json()).then(j=>{const x=(j.automations||[]).find((v:any)=>v.id===id);if(!x){setError('Automatisation introuvable.');return}setA(x);setName(x.name);setStatus(x.status)}).catch(()=>setError('Impossible de charger l’automatisation'))},[id]);async function save(e:React.FormEvent){e.preventDefault();setSaving(true);setError('');const r=await fetch(`/api/automations/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,status,trigger_type:a.trigger_type,trigger_config:a.trigger_config||{}})});const j=await r.json().catch(()=>({}));if(!r.ok)setError(j.error||'Impossible d’enregistrer');else setA(j.automation);setSaving(false)}async function remove(){if(!confirm('Supprimer cette automatisation ?'))return;const r=await fetch(`/api/automations/${id}`,{method:'DELETE'});if(r.ok)location.href='/automations';else setError('Impossible de supprimer l’automatisation')}if(error&&!a)return <AppShell active="Automations"><div className="error">{error}</div><Link href="/automations" className="outline">Retour</Link></AppShell>;return <AppShell active="Automations"><header><div><small>AUTOMATISATION</small><h1>{a?.name||'Automatisation'}</h1><p className="muted">Déclencheur : {a?.trigger_type||'—'} · Les actions ne s’exécutent que lorsque le scénario est actif.</p></div></header><section className="panel"><form onSubmit={save} style={{display:'grid',gap:16,maxWidth:620}}><label>Nom<input className="form-input" value={name} onChange={e=>setName(e.target.value)} required/></label><label>Statut<select className="form-input" value={status} onChange={e=>setStatus(e.target.value)}><option value="draft">Brouillon</option><option value="active">Active</option><option value="paused">En pause</option></select></label>{error&&<div className="error">{error}</div>}<div style={{display:'flex',gap:8}}><button className="primary" disabled={saving}>{saving?'Enregistrement…':'Enregistrer les modifications'}</button><button type="button" className="outline" onClick={remove}>Supprimer</button><Link className="outline" href="/automations">Retour</Link></div></form></section></AppShell>}
