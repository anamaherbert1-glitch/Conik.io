'use client'
import Link from 'next/link'
import {useEffect,useState} from 'react'
import {AppShell} from '@/components/app-shell'
export default function NewCampaign(){const[funnels,setFunnels]=useState<any[]>([]),[name,setName]=useState(''),[funnel,setFunnel]=useState(''),[saving,setSaving]=useState(false),[error,setError]=useState('');useEffect(()=>{fetch('/api/funnels').then(r=>r.ok?r.json():null).then(j=>setFunnels(j?.funnels||[])).catch(()=>{})},[]);async function save(e:React.FormEvent){e.preventDefault();setSaving(true);setError('');const r=await fetch('/api/campaigns',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,status:'draft',funnel_id:funnel||null})});const j=await r.json().catch(()=>({}));if(!r.ok){setError(j.error||'Impossible de créer la campagne');setSaving(false);return}location.href=`/campaigns/${j.campaign.id}`}return <AppShell active="Campaigns"><header><div><small>CAMPAGNES</small><h1>Nouvelle campagne</h1><p className="muted">Créez une définition de campagne. Les canaux de diffusion restent déconnectés tant que les intégrations ne sont pas activées.</p></div></header><section className="panel"><form onSubmit={save} style={{display:'grid',gap:16,maxWidth:620}}><label>Nom<input className="form-input" required maxLength={120} value={name} onChange={e=>setName(e.target.value)} placeholder="Ma campagne"/></label><label>Tunnel (facultatif)<select className="form-input" value={funnel} onChange={e=>setFunnel(e.target.value)}><option value="">Aucun tunnel</option>{funnels.map(f=><option key={f.id} value={f.id}>{f.name}</option>)}</select></label>{error&&<div className="error">{error}</div>}<div style={{display:'flex',gap:8}}><button className="primary" disabled={saving}>{saving?'Création…':'Créer la campagne'}</button><Link className="outline" href="/campaigns">Annuler</Link></div></form></section></AppShell>}
