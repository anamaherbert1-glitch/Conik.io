'use client'

import { LockKeyhole, Radio, ShieldCheck, Maximize2, Minimize2, MessageSquare, ChevronRight, ChevronLeft } from 'lucide-react'
import { useEffect, useRef, useState } from 'react'
import LiveRoom from '@/components/live/live-room'
import LiveChat from '@/components/live/live-chat'

type Live={id:string;title:string;description:string|null;scheduled_at:string;timezone:string;status:string;stream_provider:string|null;stream_id:string|null}

export default function PublicLive({params}:{params:Promise<{slug:string}>}){
  const[live,setLive]=useState<Live|null>(null),[slug,setSlug]=useState(''),[email,setEmail]=useState(''),[attendeeName,setAttendeeName]=useState(''),[loading,setLoading]=useState(true),[submitting,setSubmitting]=useState(false),[error,setError]=useState(''),[chatOpen,setChatOpen]=useState(true),[fullscreen,setFullscreen]=useState(false)
  const videoShellRef=useRef<HTMLDivElement>(null)
  async function markPresence(s:string){const r=await fetch('/api/lives/presence',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({slug:s}),cache:'no-store'});const j=await r.json().catch(()=>({}));if(r.ok&&j.attendee?.display_name)setAttendeeName(j.attendee.display_name)}
  useEffect(()=>{let timer:number|undefined;let active=true;(async()=>{try{const{slug:s}=await params;if(!active)return;setSlug(s);const r=await fetch(`/api/lives/access?slug=${encodeURIComponent(s)}`,{cache:'no-store'});const j=await r.json().catch(()=>({}));if(!active)return;if(r.ok&&j.authorized){setLive(j.live);await markPresence(s);if(active)timer=window.setInterval(()=>void markPresence(s),10000)}setLoading(false)}catch{if(active)setLoading(false)}})();return()=>{active=false;if(timer)window.clearInterval(timer)}},[params])
  async function requestAccess(e:React.FormEvent){e.preventDefault();setSubmitting(true);setError('');const r=await fetch('/api/lives/access',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({slug,email})});const j=await r.json().catch(()=>({}));if(!r.ok){setError(j.error||'Accès refusé.');setSubmitting(false);return}setLive(j.live);const p=await fetch('/api/lives/presence',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({slug}),cache:'no-store'});const pj=await p.json().catch(()=>({}));if(p.ok&&pj.attendee?.display_name)setAttendeeName(pj.attendee.display_name);setSubmitting(false)}
  async function toggleFullscreen(){const el=videoShellRef.current;if(!el)return;try{if(document.fullscreenElement){await document.exitFullscreen();try{await (screen.orientation as any)?.unlock?.()}catch{}}else{await el.requestFullscreen();try{await (screen.orientation as any)?.lock?.('landscape')}catch{}}}catch{setError('Le plein écran n’est pas disponible dans ce navigateur.')} }
  useEffect(()=>{const onChange=()=>setFullscreen(Boolean(document.fullscreenElement));document.addEventListener('fullscreenchange',onChange);return()=>document.removeEventListener('fullscreenchange',onChange)},[])
  if(loading)return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:24}}><p>Vérification de votre accès…</p></main>
  if(!live)return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:24}}><div className="choice" style={{maxWidth:520,width:'100%'}}><div style={{textAlign:'center'}}><LockKeyhole size={34}/><h1>Live privé</h1><p>Entrez l’adresse e-mail qui a été autorisée par l’organisateur.</p></div>{error&&<div className="error" style={{marginTop:16}}>{error}</div>}<form onSubmit={requestAccess} style={{marginTop:18}}><input className="form-input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Votre adresse e-mail"/><button className="primary" style={{width:'100%',marginTop:10}} disabled={submitting}>{submitting?'Vérification…':'Accéder au Live'}</button></form><p className="muted" style={{fontSize:12,marginTop:14,textAlign:'center'}}>Votre accès est vérifié côté serveur avant l’ouverture du Live.</p></div></main>
  const ended=live.status==='ended'
  return <main className="conik-public-live-page" style={{minHeight:'100vh',padding:'24px clamp(14px,3vw,38px)',width:'100%',maxWidth:1500,margin:'0 auto'}}><header className="conik-public-live-header" style={{padding:'18px 0 22px',display:'flex',justifyContent:'space-between',alignItems:'flex-start',gap:20}}><div style={{minWidth:0}}><span className="live"><Radio size={14}/> {ended?'LIVE TERMINÉ':live.status==='live'?'EN DIRECT':'ÉVÉNEMENT PRIVÉ'}</span><h1 style={{marginTop:10}}>{live.title}</h1>{attendeeName&&<p style={{fontWeight:700}}>Connecté en tant que {attendeeName}</p>}{live.description&&<p className="muted">{live.description}</p>}<p className="muted">{new Date(live.scheduled_at).toLocaleString('fr-FR',{dateStyle:'full',timeStyle:'short'})} · {live.timezone}</p></div></header>{ended?<section className="panel" style={{minHeight:300,display:'grid',placeItems:'center',textAlign:'center'}}><div><ShieldCheck size={42}/><h2>Ce Live est terminé</h2><p className="muted">Merci d’avoir participé.</p></div></section>:<div className="conik-follower-layout" style={{display:'grid',gridTemplateColumns:chatOpen?'minmax(0,1fr) 360px':'minmax(0,1fr)',gap:18,alignItems:'start'}}><section ref={videoShellRef} className="conik-follower-video" style={{position:'relative',minWidth:0,background:'#090a0f',borderRadius:14,overflow:'hidden'}}><LiveRoom tokenUrl="/api/lives/token" tokenBody={{slug}}/><button type="button" onClick={toggleFullscreen} aria-label={fullscreen?'Quitter le plein écran':'Mettre le Live en plein écran'} style={{position:'absolute',right:12,bottom:12,zIndex:20,minWidth:42,height:42,padding:'0 12px',border:0,borderRadius:11,background:'rgba(15,17,24,.9)',color:'#fff',display:'inline-flex',alignItems:'center',justifyContent:'center',gap:7,cursor:'pointer',boxShadow:'0 8px 22px rgba(0,0,0,.28)'}}>{fullscreen?<Minimize2 size={18}/>:<Maximize2 size={18}/>}<span style={{fontSize:12,fontWeight:700}}>{fullscreen?'Réduire':'Plein écran'}</span></button></section>{chatOpen?<section className="conik-follower-chat" style={{position:'relative',minWidth:0}}><LiveChat slug={slug}/><button type="button" onClick={()=>setChatOpen(false)} aria-label="Réduire le chat" style={{position:'absolute',left:-13,top:16,width:28,height:28,borderRadius:'50%',border:'1px solid var(--line)',background:'var(--surface)',display:'grid',placeItems:'center',cursor:'pointer',zIndex:5,boxShadow:'0 5px 16px rgba(0,0,0,.12)'}}><ChevronRight size={16}/></button></section>:<aside style={{display:'flex',justifyContent:'flex-end'}}><button type="button" onClick={()=>setChatOpen(true)} aria-label="Afficher le chat" className="outline" style={{minHeight:44,padding:'0 12px',display:'inline-flex',alignItems:'center',gap:7,fontWeight:800}}><MessageSquare size={17}/>Chat<ChevronLeft size={16}/></button></aside>}</div>}<p className="muted" style={{fontSize:12,textAlign:'center',marginTop:18}}>Accès sécurisé par Conik.io</p><style jsx>{`\n@media (max-width: 820px){\n  .conik-follower-layout{grid-template-columns:1fr !important;gap:12px !important}\n  .conik-follower-chat{order:2}\n  .conik-follower-video{order:1;width:100% !important;aspect-ratio:16/9 !important}\n  .conik-follower-video :global(.conik-live-studio-stage){width:100% !important;height:auto !important;min-height:0 !important;aspect-ratio:16/9 !important}\n}\n:fullscreen{background:#090a0f !important;padding:0 !important;border-radius:0 !important}\n:fullscreen > div:first-child{width:100%;height:100%;display:flex;align-items:center;justify-content:center}\n:fullscreen .conik-live-studio-stage{width:100% !important;height:100% !important;max-width:none !important;max-height:none !important;aspect-ratio:16/9 !important;border-radius:0 !important}\n@media (max-width: 820px) and (orientation: portrait){\n  .conik-follower-video{min-height:0 !important}\n}\n`}</style></main>
}
